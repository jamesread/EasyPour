{
  "name": "easypour-integration-tests",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "mocha --timeout 30000 'tests/**/*.spec.js'",
    "test:run": "node run-tests.js"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "selenium-webdriver": "^4.0.0"
  }
}
